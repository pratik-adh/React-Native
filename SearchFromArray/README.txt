Find whether the searched element/s exists in the array or not. If exists, rendering the element/s using flatlist.
The array is static. You can just pull a new array from API and search your required element in it. 
(Note: No npm modules used for searching)
